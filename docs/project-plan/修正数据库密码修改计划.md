# 修正数据库密码修改计划

## 问题分析
- 之前错误地将数据库密码修改为 `Zky@MySql#2025&Pro`
- 正确的数据库密码应为 `Mysql@8Root!2025`
- 需要将所有已修改的文件恢复原状，然后重新按照正确密码进行修改

## 执行步骤

### 1. 恢复已修改的文件
- 检查并恢复 `ACCOUNT_INFO.md`
- 检查并恢复 `docker-compose.yml`
- 检查并恢复 `DEPLOYMENT_DETAILED.md`
- 检查并恢复所有微服务配置文件
- 检查并恢复环境变量文件

### 2. 重新统一修改数据库密码
- 将所有配置文件中的数据库密码统一修改为 `Mysql@8Root!2025`
- 包括：
  - 账号信息文档 (`ACCOUNT_INFO.md`)
  - Docker Compose 配置 (`docker-compose.yml`)
  - 部署文档 (`DEPLOYMENT_DETAILED.md`)
  - 所有微服务配置文件 (`application.yml`)
  - 环境变量文件 (`.env.*`)

### 3. 验证修改结果
- 检查所有修改后的文件，确保密码一致
- 运行 `git diff` 查看修改内容
- 确认修改正确无误后提交

## 预期结果
- 所有配置文件中的数据库密码统一为 `Mysql@8Root!2025`
- 确保项目部署和运行时使用正确的数据库密码
- 保持配置的一致性和准确性