<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heikeji.mall.user.mapper.UserMapper">

    <select id="selectByPhone" resultType="com.heikeji.mall.user.entity.User">
        SELECT * FROM user 
        WHERE phone = #{phone} 
        AND del_flag = 0
    </select>

    <select id="selectByStudentNo" resultType="com.heikeji.mall.user.entity.User">
        SELECT * FROM user 
        WHERE student_no = #{studentNo} 
        AND del_flag = 0
    </select>

    <update id="updateBalance">
        UPDATE user 
        SET balance = balance + #{amount}, 
            update_time = NOW() 
        WHERE id = #{userId} 
        AND del_flag = 0
    </update>

</mapper>