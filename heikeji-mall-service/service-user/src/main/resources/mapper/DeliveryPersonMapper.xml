<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heikeji.mall.user.mapper.DeliveryPersonMapper">

    <select id="selectByUserId" resultType="com.heikeji.mall.user.entity.DeliveryPerson">
        select * from delivery_person where user_id = #{userId}
    </select>

    <update id="updateOrderStats">
        update delivery_person 
        set total_orders = total_orders + #{orderCount},
            rating = case when total_orders = 0 then #{ratingChange}
                          else ((rating * total_orders) + #{ratingChange}) / (total_orders + #{orderCount})
                     end,
            update_time = now()
        where id = #{id}
    </update>

</mapper>