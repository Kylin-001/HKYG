<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heikeji.mall.user.mapper.AddressMapper">

    <select id="selectByUserId" resultType="com.heikeji.mall.user.entity.Address">
        select * from address where user_id = #{userId} and del_flag = 0 order by is_default desc, update_time desc
    </select>

    <select id="selectDefaultByUserId" resultType="com.heikeji.mall.user.entity.Address">
        select * from address where user_id = #{userId} and is_default = 1 and del_flag = 0
    </select>

    <update id="cancelDefaultByUserId">
        update address set is_default = 0 where user_id = #{userId}
    </update>

</mapper>