server:
  port: 8089
  servlet:
    context-path: /api/lostfound

spring:
  application:
    name: service-lostfound
  # 禁用Nacos服务发现和配置检查，避免启动错误
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        import-check:
          enabled: false
        enabled: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.186.129:3306/heikeji_mall?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&connectTimeout=60000&socketTimeout=60000&allowPublicKeyRetrieval=true
    username: root
    password: Mysql@8Root!2025
    type: com.alibaba.druid.pool.DruidDataSource
  redis:
    host: localhost
    port: 6379
    password: Redis@hkyg
    database: 0

# Actuator配置
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

mybatis-plus:
  mapper-locations: classpath:mapper/lostfound/**/*.xml
  type-aliases-package: com.heikeji.mall.lostfound.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    com.heikeji.mall.lostfound: debug
    com.baomidou.mybatisplus: debug

# 自定义配置
lostfound:
  upload:
    path: /opt/upload/lostfound
    max-size: 10MB