server:
  port: 8007
  servlet:
    context-path: /api/lostfound

spring:
  application:
    name: service-lostfound
  main:
    allow-bean-definition-overriding: true
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration
  cloud:
    nacos:
      discovery:
        enabled: false
        server-addr: localhost:8848
        namespace: public
        ip: 127.0.0.1
      config:
        import-check:
          enabled: false
        enabled: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.186.128:3306/heikeji_mall?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&connectTimeout=60000&socketTimeout=60000&allowPublicKeyRetrieval=true
    username: hkyg
    password: Mysql@8Root!2025
    type: com.alibaba.druid.pool.DruidDataSource

# Actuator配置
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

mybatis-plus:
  mapper-locations: classpath:mapper/lostfound/**/*.xml
  type-aliases-package: com.heikeji.mall.lostfound.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    com.heikeji.mall.lostfound: debug
    com.baomidou.mybatisplus: debug

# 自定义配置
lostfound:
  upload:
    path: /opt/upload/lostfound
    max-size: 10MB