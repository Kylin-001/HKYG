<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heikeji.mall.delivery.mapper.DeliveryUserMapper">

    <select id="getByUserId" resultType="com.heikeji.mall.delivery.entity.DeliveryUser">
        SELECT * FROM delivery_user WHERE user_id = #{userId} AND del_flag = 0
    </select>

    <update id="updateStatus">
        UPDATE delivery_user SET status = #{status}, update_time = NOW() WHERE id = #{id}
    </update>

</mapper>