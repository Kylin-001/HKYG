<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heikeji.mall.payment.mapper.PaymentMapper">

    <!-- 根据订单号查询支付记录 -->
    <select id="selectByOrderNo" resultType="com.heikeji.mall.payment.entity.Payment">
        SELECT
            id,
            order_id AS orderId,
            order_no AS orderNo,
            payment_no AS paymentNo,
            payment_type AS paymentType,
            amount,
            status,
            pay_time AS payTime,
            transaction_id AS transactionId,
            create_time AS createTime,
            update_time AS updateTime
        FROM payment
        WHERE order_no = #{orderNo}
    </select>

    <!-- 根据支付流水号查询支付记录 -->
    <select id="selectByPaymentNo" resultType="com.heikeji.mall.payment.entity.Payment">
        SELECT
            id,
            order_id AS orderId,
            order_no AS orderNo,
            payment_no AS paymentNo,
            payment_type AS paymentType,
            amount,
            status,
            pay_time AS payTime,
            transaction_id AS transactionId,
            create_time AS createTime,
            update_time AS updateTime
        FROM payment
        WHERE payment_no = #{paymentNo}
    </select>
</mapper>