<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heikeji.mall.member.mapper.MemberReceiveAddressMapper">

    <select id="listByUserId" resultType="com.heikeji.mall.member.entity.MemberReceiveAddress">
        SELECT * FROM member_receive_address
        WHERE user_id = #{userId} AND del_flag = 0
        ORDER BY default_status DESC, update_time DESC
    </select>

    <select id="getDefaultAddressByUserId" resultType="com.heikeji.mall.member.entity.MemberReceiveAddress">
        SELECT * FROM member_receive_address
        WHERE user_id = #{userId} AND default_status = 1 AND del_flag = 0
        LIMIT 1
    </select>

</mapper>