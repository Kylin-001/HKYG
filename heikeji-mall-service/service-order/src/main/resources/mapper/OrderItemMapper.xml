<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heikeji.mall.order.mapper.OrderItemMapper">

    <select id="selectByOrderId" resultType="com.heikeji.mall.order.entity.OrderItem">
        SELECT * FROM order_item 
        WHERE order_id = #{orderId}
    </select>

    <select id="selectByOrderNo" resultType="com.heikeji.mall.order.entity.OrderItem">
        SELECT * FROM order_item 
        WHERE order_no = #{orderNo}
    </select>

    <select id="selectByProductId" resultType="com.heikeji.mall.order.entity.OrderItem">
        SELECT * FROM order_item 
        WHERE product_id = #{productId} 
        ORDER BY create_time DESC
    </select>

    <select id="selectByUserId" resultType="com.heikeji.mall.order.entity.OrderItem">
        SELECT oi.* FROM order_item oi
        LEFT JOIN `order` o ON oi.order_id = o.id
        WHERE o.user_id = #{userId} 
        ORDER BY o.create_time DESC
    </select>

    <insert id="batchInsert">
        INSERT INTO order_item 
        (order_id, product_id, product_name, product_image, quantity, price, total_price, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.productName}, #{item.productImage}, 
            #{item.quantity}, #{item.price}, #{item.totalPrice}, NOW(), NOW())
        </foreach>
    </insert>

</mapper>