<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heikeji.mall.takeout.mapper.TakeoutOrderMapper">

    <select id="selectByOrderNo" resultType="com.heikeji.mall.takeout.entity.TakeoutOrder">
        SELECT * FROM takeout_order 
        WHERE order_no = #{orderNo}
    </select>

    <select id="selectByUserId" resultType="com.heikeji.mall.takeout.entity.TakeoutOrder">
        SELECT * FROM takeout_order 
        WHERE user_id = #{userId} 
        ORDER BY create_time DESC
    </select>

    <select id="selectByMerchantId" resultType="com.heikeji.mall.takeout.entity.TakeoutOrder">
        SELECT * FROM takeout_order 
        WHERE merchant_id = #{merchantId} 
        ORDER BY create_time DESC
    </select>

    <select id="selectPendingOrders" resultType="com.heikeji.mall.takeout.entity.TakeoutOrder">
        SELECT * FROM takeout_order 
        WHERE status = 0 
        ORDER BY create_time ASC
    </select>

    <select id="selectByDeliveryPersonId" resultType="com.heikeji.mall.takeout.entity.TakeoutOrder">
        SELECT * FROM takeout_order 
        WHERE delivery_person_id = #{deliveryPersonId} 
        ORDER BY create_time DESC
    </select>
    
    <!-- 查询已送达但超过指定时间未完成的订单 -->
    <select id="selectOverdueDeliveredOrders" resultType="com.heikeji.mall.takeout.entity.TakeoutOrder">
        SELECT * FROM takeout_order 
        WHERE status = #{status} 
        AND actual_time < DATE_SUB(NOW(), INTERVAL #{hours} HOUR)
        AND delivery_type = 1
    </select>

</mapper>